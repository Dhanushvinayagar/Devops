
# Using Bind mount to create connection with this config file to config file od nginx in docker
# docker run -d \
# -p 9999:9999 \
# --name nginx-loadbalancer \
# -v /Users/dhanush/Documents/infra/devops/nginx/using_services/nginx.conf:/etc/nginx/nginx.conf \
# -v /Users/dhanush/Documents/infra/devops/nginx/using_services/html:/html \
# nginx

worker_processes  auto;

events {
    worker_connections  1024;
}

http {

    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream nginx_cluster {
        least_conn;
        # server localhost:9001;
        # server localhost:9002;
        server host.docker.internal:9001;
        server host.docker.internal:9002;
    }

    server {
        listen 9999;

        server_name  localhost;
        location / {
            proxy_pass http://nginx_cluster;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;

        }

        location /html {
            root /;
            index index.html index.htm;
        }

        location /page {
            alias /html/;
            index index.html index.htm;
        }
    }
}