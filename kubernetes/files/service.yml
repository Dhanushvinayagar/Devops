# Service - yml file to create a service

apiVersion: v1

kind: Service

metadata:
  name: myservice
  labels:
    app: myapp

spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000